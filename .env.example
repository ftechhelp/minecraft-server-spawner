GEMINI_API_KEY=
WEB_PANEL_URL=http://localhost:8888
SERVER_CONNECTION_HOST=localhost

IS_TEST=false

WEB_PORT=8888
MC_PORT_START=25565
MC_PORT_END=25665

CONTAINER_NAME=minecraft-spawner

COMPOSE_PROJECT_NAME=minecraft-spawner

# First admin account, created only on the very first start (when no users db exists yet)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
# Secret for session cookies; leave blank to auto-generate and persist in panel_data/
COOKIE_SECRET=
